{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader --ignore-platform-reqs"
  },
  "deploy": {
    "startCommand": "mkdir -p database && touch database/database.sqlite && chmod -R 777 database storage && php artisan migrate:fresh --seed --force && info-cmd php -S 0.0.0.0:$PORT -t public",
    "restartPolicyType": "ON_FAILURE"
  }
}
