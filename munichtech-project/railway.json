{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader --ignore-platform-reqs"
  },
  "deploy": {
    "startCommand": "mkdir -p database && touch database/database.sqlite && chmod -R 775 database storage && php artisan migrate:fresh --seed --force && php artisan serve --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
